-#
  channels.haml - Playlist object

  channels    - The item to display.
  in_channel  - If True, display a condensed channel set for display inside a
                channel object.  (Default: false.)

= haml(:json, locals: { name: :channels, item: channels }) if request.params.key? "json"
- unless request.params.key? "json"
  %section.channels
    %h1 Channels

    - if 0 < channels.channel_count
      %ol.channels-list
        = list_of(channels.channels) do |channel|
          = haml :channel, locals: { channel: channel, in_channel_list: true }
    = haml :out_links, locals: { item: channels }
